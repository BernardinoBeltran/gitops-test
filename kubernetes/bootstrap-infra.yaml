apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: root-infra-bootstrap
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/BernardinoBeltran/gitops-test.git'
    targetRevision: HEAD
    path: kubernetes/apps-infra
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
